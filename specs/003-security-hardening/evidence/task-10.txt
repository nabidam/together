Task 10 release-gate evidence

Commands exercised:

  ./scripts/verify.sh
  ./scripts/release.sh
  cd dist && sha256sum --check SHA256SUMS
  extract together_linux_amd64.tar.gz and start its binary on 127.0.0.1:18081
  ./scripts/security-e2e.sh
  ./scripts/security-e2e.sh
  TOGETHER_E2E_INJECT_FAILURE=1 ./scripts/security-e2e.sh; test "$?" -ne 0
  go test ./... -race
  go vet ./...
  gofmt -l internal cmd
  govulncheck -show verbose ./...
  git diff --check

Observed behavior:

- `verify.sh` passed Go race tests, vet, formatting, frontend tests, npm audit,
  production build, and the production security journey.
- `release.sh` created Linux amd64 and arm64 archives. Both SHA-256 checksum
  entries verified. The extracted amd64 binary booted on loopback and returned
  `ok` from `/healthz`.
- Two consecutive production security journeys passed. They exercised weak-seed
  rejection, auth throttling, invite registration, fixed-media authorization,
  WebSocket capacity, room quota/expiry/token behavior, bounded uploads, and
  restart durability.
- Injected failure after production-server startup exited non-zero and cleaned
  up its child server and temporary data directory.
- `govulncheck` found no reachable vulnerabilities. It reports module advisory
  GO-2026-5932 for unimported `golang.org/x/crypto/openpgp`; Together does not
  import or call that package.
- Independent re-review is recorded in `reviews/REVIEW_2.md`. Its two initial
  race findings were fixed and focused re-review reported no findings.
