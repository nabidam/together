Task 9 verification evidence

Commands exercised:

  ./scripts/security-e2e.sh
  TOGETHER_E2E_INJECT_FAILURE=1 ./scripts/security-e2e.sh; test "$?" -ne 0
  ./scripts/verify.sh
  git diff --check

Observed behavior:

- The production binary ran from a self-created temporary data directory and
  covered fail-closed seed, invite/register, throttled login, fixed-media WS
  control, the 12th/13th socket boundary, room expiry/quota/token behavior,
  declared-size upload, overrun and 8 MiB+1 rejection, restart durability,
  and ephemeral-room removal.
- A controlled failure after server startup exited non-zero; the trap removed
  the temporary data and stopped the server.
- Two consecutive successful production journeys printed:

  built ./together (12M)
  security-e2e: passed

- Final verifier output:

  ok   together/internal/auth   (cached)
  ok   together/internal/db     (cached)
  ok   together/internal/live   (cached)
  ok   together/internal/media  (cached)
  ℹ pass 12
  ℹ fail 0
  ✓ built in 8.56s
